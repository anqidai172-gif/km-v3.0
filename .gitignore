# Dependencies
node_modules/

# Expo
.expo/
dist/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Metro
.metro-health-check*

# Environment & Secrets
# .env.example / .env.template are OK to commit — they show structure without real keys
.env
.env.local
.env.development.local
.env.production.local
.env.test.local
*.pem
*.crt
credentials.json
service-account.json
.mcp*.json

# Database files — never commit (contain user data + API keys in settings table)
*.db
*.sqlite
*.sqlite3

# MCP / AI API keys — never commit
# (Anthropic sk-ant-*, OpenAI sk-*, DashScope/qwen, etc.)

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build
android/app/build/
ios/Pods/
ios/build/

# Claude
.claude/

# Downloaded video/audio — never commit
downloads/

# Scratch / temp files
.scratch/

# Runtime data — never commit (usage tracking, etc.)
asr_usage.json
